//Create a function that takes in an array of numbers. Return a new array containing every even number from the original array (do not use map or filter)
function takeNums(arr1){
  let arr2=[];

  arr2 = arr1;

  return arr2;
}

console.log(takeNums([1,2,3,4]));